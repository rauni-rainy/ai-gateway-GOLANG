linters:
  disable-all: true
  enable:
    - errcheck
    - govet
    - staticcheck
    - exhaustive
    - wrapcheck

run:
  timeout: 5m
  issues-exit-code: 1
  tests: false

issues:
  exclude-use-default: false
